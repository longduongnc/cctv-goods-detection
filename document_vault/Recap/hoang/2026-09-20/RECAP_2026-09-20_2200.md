# RECAP 2026-09-20 22:00

- owner: hoang
- created: 2026-09-20 22:00 +07:00
- plan/ticket đang chạy: HOANG-P01 (`draft`) · HOANG-T001 (`draft`)

Tiếp nối [RECAP_2026-09-20_1843.md](RECAP_2026-09-20_1843.md). File này phủ đoạn **18:43 → 22:00**.

## Đã đổi gì từ phiên trước

- **Tìm ra repo thật của team.** URL mà anh Long đưa (`longduongnc/cctv-goods-detection`) **đã là `origin` sẵn** của thư mục `cctv-goods-detection/`, nhánh `docs/vault-and-recap-skill`. Thư mục `milk-camera/` bên ngoài là bản trùng lặp có lịch sử git riêng, không remote — recap 18:21 đã commit nhầm vào đó.
- **Repo này là PUBLIC trên GitHub.** Xác nhận bằng `gh repo view`: `isPrivate: false`. Không được ghi IP, hostname, tên tài khoản vào bất cứ file nào trong repo.
- **Có quyền dùng máy GPU của anh Long.** Job train đang chạy trên đó là **việc Kaggle riêng của hoang**, không thuộc dự án này — nên không phải giữ ý về việc chạy chồng.

## Đã xong

### 1. Chạy được chặng S1 — gỡ được việc chặn đường số 1

Máy GPU Linux của team: RTX 5090 32 GB, torch 2.11.0+cu128, ffmpeg 6.1.1, 502 GB trống.

**Máy là Linux, không phải Apple Silicon** — nên `tools/locate_video.py` vẫn không chạy nguyên trạng. Giải bằng bản gốc `nvidia/LocateAnything-3B` trên Hugging Face, chạy `batch_infer.py` có sẵn với `--attn sdpa`.

Bốn lỗi môi trường đã gỡ, ghi đầy đủ ở trang research:

| Triệu chứng | Cách gỡ |
|---|---|
| `No module named 'batch_utils'` | `batch_infer.py` trong HF cache là symlink sang `blobs/` → đặt `PYTHONPATH=$SNAP` |
| thiếu `decord`, `peft` | `pip install` |
| `No module named 'transformers.models.qwen3'` | transformers 4.46 quá cũ |
| `_check_and_adjust_attn_implementation() ... 'allow_all_kernels'` | transformers 5.17 **quá mới** → ghim **4.51.0** (lấy từ `generation_config.json` của model) |

Dùng **venv riêng** (`--system-site-packages`) nên transformers hệ thống vẫn nguyên 4.46.3, job train không bị đụng — đã kiểm lại sau khi chạy.

**Chất lượng định vị tốt:** `milk bottle` → 52 box ôm sát từng chai/hộp/can trên kệ Almarai. `item in hand` → đúng 1 box nằm trọn trên hộp sữa đang cầm.

**Chọn chữ cho prompt là tham số thật:** cùng khung hình, `milk bottle` 52 box vs `product` 19 box.

**Thông lượng: ~42 giây/khung hình** (GPU chia với job train).

### 2. Đối chứng âm — LocateAnything KHÔNG nhận được hãng

| Hỏi gì | Có thật trên kệ? | Số box |
|---|---|---|
| `Almarai milk` | **Có** | 15 |
| `Vinamilk milk carton` | **Không hề có** | **43** |
| `Dutch Lady milk carton` | **Không hề có** | **41** |

Vẽ box ra ảnh: `Almarai` và `Vinamilk` rơi vào **đúng cùng những vật thể**. Model bỏ qua tên hãng. Loại và lẻ/lô cũng không phân biệt được (`chocolate milk` → 0 box dù có nguyên lô; `multipack of milk cartons` → 0 box).

### 3. Chạy được chặng S4 — đọc nhãn bằng model khác

`Qwen2.5-VL-7B-Instruct` nạp 4-bit NF4 (vừa ~15,6 GB VRAM còn trống), 11 ảnh cắt theo box của S1:

| Đo gì | Kết quả |
|---|---|
| Hãng đúng | **8/11** |
| Tự nhận `unknown` | 3/11 |
| **Hãng bịa** | **0/11** |
| Đơn vị bán (lẻ/lô) đúng | **9/11** |
| Tốc độ | **3,2 giây/ảnh cắt** |

Đọc được `18 x 200ml` trên thùng Double Chocolate Milk → trả `pack_count: 18`.

Ba cảnh báo đã ghi vào plan: `text_read` chữ Ả Rập **có chỗ bịa** (đọc ra "500 lít" cho can 2 lít) · `confidence` chưa hiệu chuẩn (báo 0,9 kèm chữ bịa) · loại mới đúng ở mức thô, chưa tách được dung tích hay % béo.

### 4. Đã sửa hai chỗ tôi nói quá ở bản plan 18:36

- **Ràng buộc không gian cho nhánh tay**: lý do hẹp hơn đã viết. Trên ảnh kệ sữa thật, `item in hand` cho đúng 1 box đúng chỗ. Lỗi bùng nổ 25–35 box có vẻ đặc thù cảnh MERL (lưới hàng dày đặc giống hệt nhau). Giữ ràng buộc nhưng hạ xuống mức *nên có*.
- **Câu "nhãn đọc rõ nên S4 kiểm thử được ngay"** là sai — nhãn rõ với mắt người, `LocateAnything` thì không đọc được.

Cũng phát hiện output thô có 341 box nhưng **bộ lọc sẵn có trong `locate_video.py` hấp thụ trọn** (341 → 2 → 1). Lo lắng ban đầu của tôi về chuyện này là thừa.

### 5. Tài liệu

| Trang | id | Việc |
|---|---|---|
| Research — chạy được S1 + đối chứng âm | `131615` | tạo mới, v2 |
| Research — đọc nhãn bằng Qwen2.5-VL-7B | `131632` | tạo mới |
| HOANG-P01 | `66482` | v4 → v5 |
| HOANG-T001 | `98895` | v2 → v4 |
| Cách dùng — RoadMap / Ticket / Research | `98529` `65928` `98546` | chỉ mục + mốc thời gian |

## Đang dở

**Không có job nào đang chạy cho dự án này.** Job train Kaggle riêng vẫn chạy trên máy GPU.

Môi trường đã dựng trên máy GPU, **để nguyên cho phiên sau dùng lại**:

```
~/hoang-s1/venv/                 venv, transformers==4.51.0 + peft + bitsandbytes + qwen-vl-utils
~/hoang-s1/frames/               6 khung hình thô
~/hoang-s1/crops/                11 ảnh cắt theo box S1
~/hoang-s1/s4_label.py           script chấm nhãn
~/hoang-s1/out_s4.jsonl          16 truy vấn đối chứng âm
~/hoang-s1/out_s4_label.jsonl    kết quả đọc nhãn
~/.cache/huggingface/hub/        LocateAnything-3B (7,2 GB) + Qwen2.5-VL-7B (16 GB)
```

Cài thêm vào `~/.local` của máy đó: `decord` (dùng `--break-system-packages`, chỉ ghi user-site, không đụng python hệ thống).

**`document_vault/RESOURCES.md` đang sửa, chưa commit** — xem mục *Câu hỏi treo*.

## Bước kế tiếp

**Việc đầu tiên của phiên sau: tải bản MERL Shopping đầy đủ từ nguồn gốc, kiểm điều kiện 4 của HOANG-T001 trên một đoạn dài** — xem hộp có được trả lại kệ hay đi luôn.

Đây giờ là **việc chặn đường duy nhất**: phần model đã xong cả hai đầu, chỉ thiếu dữ liệu. Nếu MERL đầy đủ đạt điều kiện 4 thì **Pha 1 chạy được ngay**.

Sau đó, theo thứ tự:

1. **Đưa sẵn danh mục SKU vào câu hỏi của S4** thay vì hỏi mở "hãng gì". Danh mục đó chính là *danh mục loại sữa* đã có trong S0. Đây là việc rẻ nhất có khả năng tăng độ chính xác.
2. **Dò prompt cho S1 một cách có hệ thống** — mới biết `milk bottle` hơn `product`, chưa thử `milk`, `dairy product`, `bottle on shelf`.
3. Tìm YouTube bằng từ khoá hướng camera an ninh.
4. Khi GPU rảnh: thử `Qwen3-VL-32B-Instruct-AWQ` (đã có sẵn trong cache máy đó) để biết 7B còn cách bao xa.

## Quyết định đã chốt

- **Hai chặng dùng hai model khác nhau.** S1 định vị bằng `LocateAnything-3B`, S4 đọc nhãn bằng `Qwen2.5-VL-7B`. Đã có số cho cả hai. Đừng thử lại hướng "một model làm cả hai".
- **Ghim `transformers==4.51.0`**, không nâng, không hạ. Cả trên lẫn dưới đều gãy.
- **Dùng venv riêng, không đụng môi trường hệ thống của máy GPU.**
- **Không ghi thông tin truy cập máy vào repo** vì repo public. Để ở Google Sheet.
- **Giữ `status` = `draft` cho cả plan lẫn ticket.** Reviewer chưa duyệt; Claude rule cấm agent tự duyệt.
- **Không dùng `text_read` và `confidence` của S4 làm bằng chứng** cho tới khi hiệu chuẩn lại.
- **Chuẩn hoá tên hãng trước khi so khớp ở S5** — S4 trả `المراعي` ở ảnh này và `Almarai` ở ảnh khác, cùng một hãng.
- **"8/11" không phải con số báo cáo được.** 11 ảnh quá ít, chuẩn vàng do chính người viết pipeline tự đọc.

## Câu hỏi treo

- **Chờ anh quyết: có commit `document_vault/RESOURCES.md` không?** File đang sửa, chưa commit. Tôi đã bỏ IP, hostname, tên tài khoản. Nhưng phần "ai đang giữ máy nào, chạy job gì" hợp với **Google Sheet** hơn là một repo public — đúng như CLAUDE.md nói *việc và link → Sheet*.
- **Chờ long — hai câu** (câu về máy chạy S1 đã tự gỡ được, đã bỏ):
  1. Ngoài hai link trong Sheet, có sẵn video nào khác không — đặc biệt là loại camera đặt cố định?
  2. Ba clip MERL trong `media/` chạy với tham số gì (`--every`, ngưỡng điểm tin)?
- **Chờ cả team** — chốt chỗ lưu video thô.
- **Chưa hỏi ai:** ai quen chủ tiệm tạp hoá để xin quay 10–15 phút kệ sữa.
- **Chưa ai trả lời:** mốc 28/09 là hạn cứng hay ước lượng.
- **Chưa hỏi:** thư mục `milk-camera/` bên ngoài có còn ai dùng không — nó đang giữ hai commit recap mồ côi (`a6ab417`, `19878cb`).
- **Chưa giải:** license `nvidia/LocateAnything-3B` vẫn là **phi thương mại** (*"for research and development only"*). Chạy được không gỡ được rủi ro bàn giao.

## File đã sửa

Trong repo `cctv-goods-detection`, nhánh `docs/vault-and-recap-skill`:

- `document_vault/Recap/hoang/2026-09-20/RECAP_2026-09-20_2200.md` (file này, mới)
- `document_vault/RESOURCES.md` (**đang sửa, chưa commit** — chờ anh quyết)

Không sửa file code nào. Bảy trang Confluence đã liệt kê ở mục *Đã xong*.

## commit

- commit: `<điền sau khi push>`
