# RECAP 2026-09-20 18:43

- owner: hoang
- created: 2026-09-20 18:43 +07:00
- plan/ticket đang chạy: HOANG-P01 (`draft`) · HOANG-T001 (`draft`)

## Đã đổi gì từ phiên trước

- **Không ai đổi gì** — đây là recap đầu tiên của hoang. Phiên trước kết thúc mà chưa viết recap, nên không có gì để so.
- **Không plan/ticket nào của người khác đang `in-progress` hoặc `blocked`.** Bằng chứng: ba bảng chỉ mục ở [Cách dùng — RoadMap](https://traditional-milk.atlassian.net/wiki/spaces/~7120204f8c1fdfbeb646c882494b9ab507ea3f/pages/98529) (chỉ có HOANG-P01), [Ticket](https://traditional-milk.atlassian.net/wiki/spaces/~7120204f8c1fdfbeb646c882494b9ab507ea3f/pages/65928) và [Research](https://traditional-milk.atlassian.net/wiki/spaces/~7120204f8c1fdfbeb646c882494b9ab507ea3f/pages/98546) đều trống lúc 18:05.
- **Sheet không đổi việc của tôi:** *"lên kế hoạch thiết kế quy trình phân tích dữ liệu"*, P0, `Đang thực hiện`, 19/09 → 28/09. Khang / nghi / ngọc research thị trường và khía cạnh khai thác dữ liệu; nhật lên ý tưởng software — không việc nào đụng vào việc tôi đang làm.

## Đã xong

### 1. Kiểm chứng hai video YouTube trong Sheet — không dùng được

Việc mà plan ghi là "việc kế tiếp ngay". Tải cả hai, cắt **21 + 58 khung hình** (mỗi giây một khung, phủ trọn cả hai), xem bằng mắt.

**Giả định "camera cố định" trượt trên cả hai.** Đều là vlog quay tay, camera lia liên tục. `rBSgQkBbz9U` không có người nào trong 21 giây; `3CkllRqVcaY` có 4 lần giơ hộp khoe máy quay (t≈18, 24, 35, 46), không phải hành vi mua. Hệ quả: S0 không vẽ được vùng kệ → nhánh kệ ở S3 sụp → S5 mất đối chiếu chéo; luật `che_khuat` vô nghĩa; chỉ 4–5 sự kiện so với mốc ≥30.

Nhãn thương hiệu **đọc rất rõ** ở cả hai — đó là phần duy nhất đạt.

→ [Trang research 98873](https://traditional-milk.atlassian.net/wiki/spaces/~7120204f8c1fdfbeb646c882494b9ab507ea3f/pages/98873)

### 2. Kiểm ba clip MERL trong `media/` — thoả 3/4 điều kiện

Ba clip 7 giây, 640×472. Lấy **14 khung mỗi clip**, cách nhau 0,5 giây.

| Điều kiện HOANG-T001 | Kết quả |
|---|---|
| Camera không di chuyển | **Đạt** — kệ, sàn, thiết bị đứng yên tuyệt đối qua cả 14 khung |
| Thấy kệ, kệ nằm yên trong khung | **Đạt** — lưới hàng xếp đều, phân biệt được từng ô |
| Có người thật lấy hàng | **Đạt** — cả ba clip đều có |
| Thấy được kết cục | **Không đạt** — clip cắt còn 7 giây, kết thúc khi vật còn trong tay |

Điều kiện 4 trượt là giới hạn của **bản cắt**, không phải của dataset gốc.

### 3. Hai phát hiện ngoài dự tính — quan trọng hơn cả việc đếm điều kiện

**a. Prompt `item in hand` bùng nổ dương tính giả, đúng lúc cần nhất.**

Số đếm `item in hand` qua 14 khung mỗi clip:

| Clip | Diễn biến |
|---|---|
| `merl-1_1` | 0 → 1, giữ 1 tới hết. Sạch |
| `merl-21_1` | 0 ↔ 1 đổi qua lại theo từng lần với tay. Sạch |
| `merl-31_1` | 0,0,0,0 → **30, 31, 27, 35** → 1,1,1,1 → **28**, 0 |

Bốn khung bùng nổ của `merl-31_1` (2,0–3,5 giây) rơi **đúng đoạn người đứng sát kệ và đang với tay lấy hàng** — đúng thời điểm S3 cần tín hiệu này nhất. Box giả vẽ trùm lên lưới hàng trên kệ, gần trùng với chính các box mà `product` bắt được.

Hệ quả nặng hơn con số: **hai nguồn được coi là độc lập ở S5 thực ra đang bắt cùng một tập vật thể.** Nguyên tắc *"chỉ ghi tự động khi hai nguồn độc lập cùng nói một điều"* mất hiệu lực đúng lúc xảy ra sự kiện.

Đối chứng sạch: `merl-1_1` tại 3,0 giây — `person 1` · `item in hand 1` nằm gọn **bên trong** box người · `product 25` đúng 25 ô trên kệ. Đúng y như thiết kế trông đợi.

→ Đã thêm vào HOANG-P01 một luật mới: **ứng viên *vật trong tay* chỉ tính khi box chồng lên box `person` quá một ngưỡng.** Số tham số phải đo ở pha 1 tăng từ ba lên bốn.

**b. `tools/locate.py` và `tools/locate_video.py` chỉ chạy trên Apple Silicon.**

Cả hai hardcode `MODEL_ID = 'mlx-community/LocateAnything-3B-4bit'` và import `mlx_vlm` / `mlx.core`. `locate.py` dòng 12 ghi thẳng *"Apple-Silicon build of"*. Kiểm gói trên máy Windows đang dùng:

```
mlx False · mlx_vlm False · torch False · transformers False · ultralytics False
```

**Không chạy được dòng nào của S1 trên máy này.** Điều này làm câu hỏi tôi đặt cho long ở bản plan trước trở nên sai trọng tâm — nó hỏi *model có chạy trên video dọc không*, câu đúng phải là *ai có máy chạy được S1*.

→ [Trang research 66517](https://traditional-milk.atlassian.net/wiki/spaces/~7120204f8c1fdfbeb646c882494b9ab507ea3f/pages/66517)

### 4. Tìm ra repo thật của team

`git remote -v` ở thư mục `milk-camera` không in ra gì, nhưng `cctv-goods-detection/` bên trong **đã có sẵn** `origin = https://github.com/longduongnc/cctv-goods-detection.git`, nhánh `docs/vault-and-recap-skill`, và **đã chứa `document_vault`** (commit `4c48476`).

Nghĩa là `milk-camera` ở ngoài là **bản trùng lặp có lịch sử git riêng, không remote**. Recap tôi viết lúc 18:21 đã commit nhầm vào đó (`a6ab417`, `19878cb`) — hai commit đó nằm lại trong bản trùng lặp, không push đi đâu được. File recap này thay thế nó và đặt đúng chỗ.

### 5. Tài liệu đã cập nhật

| Trang | id | Việc |
|---|---|---|
| HOANG-P01 | `66482` | v2 → v4: thêm ràng buộc không gian S3, thêm hai dòng Chỗ dễ gãy, mục S1-Apple-Silicon, cập nhật Pha 0 |
| HOANG-T001 | `98895` | tạo mới, rồi v1 → v2 thêm mục Tiến độ |
| Research — hai video YouTube | `98873` | tạo mới |
| Research — MERL | `66517` | tạo mới |
| Cách dùng — RoadMap | `98529` | chỉ mục, v4 → v6 |
| Cách dùng — Ticket | `65928` | chỉ mục, v4 → v6 |
| Cách dùng — Research | `98546` | chỉ mục, v3 → v5 |

## Đang dở

**Không có job nào đang chạy, không có file nào sửa giữa chừng.**

Dữ liệu tạm của phiên (sẽ mất khi máy dọn temp, tải lại được — lệnh ghi ở mục *Bằng chứng* của hai trang research):

```
<scratchpad>/video/rBSgQkBbz9U.f248.webm   sha256 a3378058066458119f6ec48c58dc18042af6c3e95fab3ee8f650e0293525a91b
<scratchpad>/video/3CkllRqVcaY.f248.webm   sha256 aea36636ccd6c6c89b29867b7e5748e99005656b5f9678c3d44ed5ba0862a00f
<scratchpad>/frames/   21 + 58 khung từ hai video YouTube
<scratchpad>/merl/     14 khung × 3 clip MERL
```

**Không commit vào repo** (Claude rule mục 9); `.gitignore` cũng chặn `*.mp4` / `*.jpg`.

Môi trường đã cài trong phiên: `yt-dlp 2026.08.19` qua `python -m pip`. `ffmpeg` gọi qua `imageio_ffmpeg.get_ffmpeg_exe()` — không cài ffmpeg hệ thống.

## Bước kế tiếp

**Việc đầu tiên của phiên sau: tải bản MERL Shopping đầy đủ từ nguồn gốc và kiểm điều kiện 4 trên một đoạn dài** — xem hộp có được trả lại kệ hay đi luôn. Đây là việc duy nhất còn lại **không cần chạy model và không chờ ai**.

Nguồn: Singh, Marks, Jones, Tuzel, Shao, *A Multi-Stream Bi-Directional Recurrent Neural Network for Fine-Grained Action Detection*, CVPR 2016. `README.md` ghi *"Datasets are not included. Download them from their original sources."*

Sau đó, theo thứ tự:

1. Chốt lối chạy S1 (cần long) — ba lối đã liệt kê trong HOANG-P01: mượn máy Mac trong team · viết lại phần nạp model sang `torch` + bản gốc `nvidia/LocateAnything-3B` (máy này có GPU NVIDIA và CUDA 12.3) · đổi hẳn model khác.
2. Tìm YouTube bằng từ khoá hướng camera an ninh (`cctv grocery store`, `store security camera milk`), không phải vlog.

## Quyết định đã chốt

- **Giữ `status` = `draft` cho HOANG-P01, không chuyển `blocked`.** Plan chưa từng chạy nên chưa thể gọi là tắc; thiết kế đang được sửa chứ không đứng yên chờ.
- **Không tự chuyển HOANG-T001 sang `approved`.** Claude rule cấm agent tự duyệt ticket của chính mình.
- **Không bác bỏ thiết kế HOANG-P01.** MERL cho thấy khi điều kiện được thoả thì tín hiệu ra đúng như thiết kế trông đợi. Sơ đồ bảy chặng giữ nguyên.
- **Thêm ràng buộc chồng box tay ↔ người vào S3** thay vì hạ ngưỡng điểm tin. Lý do: vấn đề không phải nhiễu mà là hai nguồn mất tính độc lập — ràng buộc không gian mới khôi phục được tính độc lập đó.
- **Bỏ dòng "thử S4 trên ảnh tĩnh" khỏi Pha 0.** Đề xuất này ở bản plan 18:15 là sai: S4 cũng cần VLM, mà máy này không có `mlx_vlm` lẫn `torch`. Đã sửa tại chỗ trong plan.
- **Làm việc trong `cctv-goods-detection/`, không phải `milk-camera/`.** Đó mới là repo có remote và có `document_vault` của team.
- **Lưu video thô ở thư mục tạm, không vào repo** (Claude rule mục 9). Chỗ lưu lâu dài cho cả team vẫn chưa chốt.

## Câu hỏi treo

- **Chờ long — ba câu**, đã ghi ở cuối Nextstep của HOANG-P01 và ở HOANG-T001:
  1. **Ai trong team có máy Apple Silicon chạy được S1?** Hoặc đã có bản không-MLX chưa? *(Đây là việc chặn đường số 1.)*
  2. Ngoài hai link trong Sheet, có sẵn video nào khác không — đặc biệt là loại camera đặt cố định?
  3. Ba clip MERL trong `media/` chạy với tham số gì (`--every`, ngưỡng điểm tin)? Số đếm bùng nổ có thể chỉ vì chạy không đặt ngưỡng.
- **Chờ cả team** — chốt chỗ lưu video thô, trước khi có video thật đầu tiên.
- **Chưa hỏi ai, nhưng cần hỏi:** ai quen chủ một tiệm tạp hoá để xin quay 10–15 phút kệ sữa? Đây là hướng duy nhất cho dữ liệu đúng thị trường Việt Nam.
- **Chưa ai trả lời:** mốc 28/09 là hạn cứng hay ước lượng. *Project plan* và Sheet vẫn đang ghi như hạn cứng.
- **Chưa hỏi:** thư mục `milk-camera/` ở ngoài có còn ai dùng không, hay bỏ được. Nó đang giữ hai commit recap mồ côi.

## File đã sửa

Trong repo `cctv-goods-detection` (nhánh `docs/vault-and-recap-skill`):

- `document_vault/Recap/hoang/2026-09-20/RECAP_2026-09-20_1843.md` (file này, mới)

Không sửa file code nào. Bảy trang Confluence đã liệt kê ở mục *Đã xong*.

## commit

- commit: `087ad0db94b6ef38b56118584c108197549fb01c` — đã push lên `origin/docs/vault-and-recap-skill` (`4c48476..087ad0d`).
