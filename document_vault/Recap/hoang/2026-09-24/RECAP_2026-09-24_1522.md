# RECAP 2026-09-24 15:22

- owner: hoang
- created: 2026-09-24 15:22 +07:00
- plan/ticket đang chạy: HOANG-T005 (`in-progress`, mở phiên này) · HOANG-P03 (`in-progress`) · HOANG-P01 (`draft`) · HOANG-T001 (`draft`). Đóng trong phiên: HOANG-T004 `done`.

Tiếp nối [RECAP_2026-09-24_1048.md](RECAP_2026-09-24_1048.md). File mở lúc 15:22 với ba dòng đầu phiên; phần còn lại viết lúc chốt phiên, 16:11. Phiên này làm hai việc. Việc thứ nhất là đóng HOANG-T004. Việc thứ hai là gom mọi thứ đã có thành trang chung *Proposal* cho supervisor, qua ticket HOANG-T005.

## Đã đổi gì từ phiên trước

- **Người khác: không đổi gì.** `git fetch --all --prune` lúc 15:18 không kéo về commit nào. CQL `lastmodified >= now("-48h")` lúc 15:20 ra 15 trang, cả 15 là trang hoang sửa. Claude rule vẫn v2.1. Phiên trước **chưa commit và push**: recap 10:48 chưa được git theo dõi, bốn file `docs/architecture/` vẫn ở trạng thái `M`.
- **Sheet ProjectA (sửa lần cuối 12:52 +07:00) khác với recap 10:48:** dòng *architecture design* giờ có chủ hoang, P0, `Đang thực hiện`, 24/09 → 30/09 (trước trống). Dòng *prototype* là của nhật, P1 (recap 10:48 ghi hoang, P0). Dòng MeetingNote 24/09 thêm Nhật vào danh sách người dự, và thêm hai quyết định "thiết kế thêm 1 trang problems and risks" và "Update lại architecture". Dòng *Proprosal* P0 thuộc Long và Ngoc, 24/09 → 30/09, ghi chú "bản prototype demo". Không có dòng này ở recap 10:48, nhưng P03 đã nhắc tới lúc 11:52.
- **hoang báo đầu phiên:** muốn gom mọi thứ thành một proposal dễ hiểu, trình bày trong một trang cho supervisor xem.

## Đã xong

1. **Làm đủ năm bước đầu phiên** và đọc lại Claude rule (v2.1, phiên bản trang 8). MeetingNote 19/09 cho thấy team đã gặp supervisor một lần ("Thảo luận các hướng đi với supervisor").
2. **Đóng HOANG-T004** `2523154` → `done` (v3, 15:27). Căn cứ đóng là dòng MeetingNote 24/09 trong Sheet có quyết định *"Update lại architecture"*; hoang xác nhận dòng đó đủ làm thông báo. Gạch việc 2 và 3 trong Nextstep, thêm Nhật ký. Ở HOANG-P03 (v3, 15:28): gạch Nextstep 1, thêm Nhật ký.
3. **Mở HOANG-T005 — Viết trang chung Proposal cho supervisor** `2523187`. Tạo lúc 15:31 ở trạng thái `in-progress`, reviewer là hoang tự duyệt, long và ngọc góp ý. Mục *Ticket liên quan* ghi rõ ticket này đè lên việc *Proprosal* của Long và Ngọc trong Sheet. Lần sửa 15:37 (v2): gạch Nextstep 1, thêm Nhật ký.
4. **Tạo trang chung [Proposal](https://traditional-milk.atlassian.net/wiki/spaces/~7120204f8c1fdfbeb646c882494b9ab507ea3f/pages/2523213/Proposal)** `2523213` dưới Overview `65797` lúc 15:33 (v1). Viết bằng tiếng Việt cho supervisor, gồm 11 mục:
   - 1\. tóm tắt
   - 2\. bài toán (4 trong 12 KPI, vì sao đổi hướng ngày 24/09)
   - 3\. phạm vi team và Datagent
   - 4\. giải pháp 4 bước bằng lời thường, cộng 3 việc chạy song song
   - 5\. bảy lựa chọn chính kèm số liệu
   - 6\. chi phí ($444/tháng, $28.470 tiền camera, 4G)
   - 7\. đã có và chưa có
   - 8\. năm rủi ro lớn nhất
   - 9\. cần quyết và cần hỏi (9a Datagent · 9b team · 9c supervisor)
   - 10\. kế hoạch
   - 11\. tài liệu gốc

   Đọc lại bằng `getConfluenceContent` (markdown): đủ 11 mục, bảng và link thẻ hiện đúng tên trang. Kết quả báo `lossyConversion: ["ac:structured-macro"]`, nhưng đó chỉ là panel và ô trạng thái bị mất khi xuất sang markdown, không phải lỗi của trang. Có hai chỗ thêm ngoài dàn ý đã duyệt, cả hai lấy từ P03: việc *Theo dõi camera* ở mục 4, và dòng *Đặt hệ thống ở Tokyo* ở mục 5 (để giải thích rủi ro pháp lý ở mục 8).
5. **Commit và push phần phiên 10:48 để lại** cùng recap này (hash ở mục *commit*). Trước đó đã quét bằng `grep` tìm secret, token, IP, `AKIA`, `@gmail` trong các file sẽ commit. Chỉ khớp hai dòng khoá giá `secret_mo` (giá Secrets Manager) trong `cost_model.py`; file `.drawio` ra 0 kết quả.

| Trang | id | Việc | Phiên bản |
|---|---|---|---|
| HOANG-T004 | `2523154` | `done`, Nextstep 2–3, Nhật ký | v3 |
| HOANG-P03 | `2392147` | Gạch Nextstep 1, `updated` 15:28, Nhật ký | v3 |
| HOANG-T005 | `2523187` | Tạo 15:31; Nextstep 1 xong 15:37 | v2 |
| Proposal | `2523213` | Tạo dưới Overview 15:33 | v1 |
| Cách dùng — Ticket | `65928` | T004 `done` (v13); thêm dòng T005 (v14) | v14 |

## Đang dở

- **HOANG-T005 còn `in-progress`.**
  - **Nextstep 2 do hoang sửa tay, chưa biết đã làm chưa:** sửa dòng *Proprosal* trong Sheet (thêm hoang vào Chủ sở hữu, trỏ Thành phẩm tới trang Proposal), rồi báo Long và Ngọc. Agent không sửa ô Sheet qua connector vì dễ ghi đè cả file. Nội dung ghi chú gợi ý đã đưa hoang trong chat:
    > Proposal cho supervisor: trang chung Proposal dưới Overview, gom yêu cầu Datagent, giải pháp, chi phí ~$444/tháng, 5 rủi ro lớn và câu hỏi cần quyết. Bản nháp, chờ Long và Ngọc góp ý. Ticket HOANG-T005.
  - **Nextstep 3:** Long và Ngọc góp ý trước 30/09.
  - **Nextstep 4:** đổi trạng thái trang từ bản nháp sang "gửi supervisor", gửi link, rồi đóng ticket.
- **Local-id để sửa trang Proposal sau này:** đoạn có dòng *Người viết · Trạng thái · Cập nhật* ở đầu trang là `e1a2b3c4d5f6` (ô trạng thái ghi `BẢN NHÁP`); mục Nhật ký cuối trang là `f7e6d5c4b3a2`. Các khối khác đọc HTML để lấy local-id. Không ghi đè cả trang bằng markdown, vì trang có panel và ô trạng thái.
- **Chưa kiểm supervisor có quyền vào `traditional-milk.atlassian.net` chưa.** Chưa có quyền thì không gửi link được.
- **Dòng T005 trong chỉ mục *Cách dùng — Ticket*** ghi *Cập nhật* 15:31, trong khi ticket sửa lần cuối lúc 15:37. Sửa khi đóng ticket.
- **HOANG-P03 còn `in-progress`**, giữ nguyên như recap 10:48:
  - Nextstep 2 còn hai sơ đồ `milk-aws-compute-detail` và `milk-aws-security`. Cả hai vẫn là bản của P02.
  - Nextstep 3 là các câu hỏi cho Datagent, hạn trước 28/09. Các câu này giờ đã gom vào mục 9a của Proposal.
- **File draw.io trên Google Drive mà Sheet trỏ tới** vẫn là bản cũ. Chưa hỏi lại hoang ai sẽ thay.
- **Còn chưa kiểm chứng:**
  - chỉ có S3 gateway endpoint đã đủ cho SageMaker Async khi bật network isolation chưa;
  - 30 giây/ảnh trên L4 chưa đo;
  - giá Viettel mới lấy từ trang đại lý.
- `document_vault/RESOURCES.md` vẫn sửa dở từ 20/09, chưa commit (phiên này không đụng).

## Bước kế tiếp

**Việc đầu tiên: hỏi hoang ba câu.**
1. Đã sửa dòng *Proprosal* trong Sheet và báo Long, Ngọc chưa?
2. Supervisor đã có quyền vào Confluence chưa?
3. Long và Ngọc góp ý gì cho Proposal?

Nếu có góp ý thì sửa trang Proposal `2523213` theo local-id, sửa dòng *Cập nhật* ở đầu trang, thêm Nhật ký ở trang đó và ở HOANG-T005. Khi hoang cho gửi supervisor: đổi ô trạng thái `BẢN NHÁP` (đoạn `e1a2b3c4d5f6`), gạch Nextstep 4, chuyển T005 sang `done` và sửa dòng T005 trong *Cách dùng — Ticket*.

Sau đó quay lại *Bước kế tiếp* của [recap 10:48](RECAP_2026-09-24_1048.md): vẽ lại sơ đồ chi tiết tầng xử lý (`build_milk_compute.mjs`). Đưa hoang duyệt danh sách nội dung sơ đồ trước khi viết script.

## Quyết định đã chốt

- **Trang Proposal là Proposal chung của team**, tức chính là việc *Proprosal* trong Sheet. hoang viết, Long và Ngọc duyệt và bổ sung. hoang chọn phương án này trong ba phương án agent đưa ra; hai phương án kia là "nháp để Long, Ngọc dùng" và "hai việc khác nhau". Lý do: tránh gửi supervisor hai bản proposal khác nhau.
- **Đặt trên Confluence thành trang chung dưới Overview**, không làm trang web riêng (Artifact), không làm cả hai. hoang chọn. Lý do: Long và Ngọc sửa được, và link thẳng tới các trang nguồn. Hệ quả: supervisor phải có quyền vào site.
- **Dàn ý 11 mục được hoang duyệt ("ok").** Viết cho supervisor. Phần chính không dùng tên service AWS. Không thêm kết luận mới, giữ nguyên nhãn *(chưa kiểm chứng)*. Sơ đồ chỉ viết bằng chữ, không nhúng ảnh draw.io.
- **Ba điểm xin ý kiến supervisor (mục 9c):**
  - (1) chỉ đếm tồn kho có đúng trọng tâm không, và có giữ nhánh thị trường/giá trị dữ liệu của khang, nghi, ngọc không;
  - (2) $444/tháng và $28.470 có hợp lý không, và ai trả;
  - (3) ai hỗ trợ phần pháp lý theo Luật 91/2025.
- **Đóng T004 bằng dòng MeetingNote "Update lại architecture".** hoang xác nhận dòng đó đủ làm thông báo, không cần ghi thêm vào Sheet.

## Câu hỏi treo

- **hoang:**
  - Sửa dòng *Proprosal* trong Sheet và báo Long, Ngọc.
  - Supervisor đã có quyền vào Confluence chưa.
  - Thay file draw.io trên Drive (tự làm hay để agent làm).
  - Có vẽ tiếp sơ đồ chi tiết xử lý và sơ đồ bảo mật không.
  - Chọn đơn vị đếm.
  - Có commit `RESOURCES.md` không (treo từ 20/09).
  - Câu "dòng *architecture design* trong Sheet trống chủ" từ phiên trước đã tự giải quyết: giờ là hoang, `Đang thực hiện`.
- **long, ngọc:** góp ý Proposal trước 30/09, và có đồng ý để việc *Proprosal* thành trang chung do hoang viết không.
- **supervisor (qua hoang):** ba câu ở mục 9c của Proposal.
- **Datagent (qua hoang, hạn trước 28/09):** các câu ở mục 9a của Proposal, gồm:
  - dữ liệu và top 50 SKU;
  - ai lắp camera, số tiệm không có WiFi, tiệm cần 2 camera, giờ đóng cửa;
  - tần suất chụp và thời gian giữ ảnh;
  - ai giữ tài khoản AWS và ngân sách, ai trả tiền;
  - bounding box, định nghĩa "kệ", cách chấm khi người đếm ra 0, và `processing_time_ms`.
- **Tư vấn pháp lý:** chưa giao ai, treo từ 23/09.
- **Luxonis:** nguồn hàng, giá và thời gian giao OAK 4 S về Việt Nam (treo từ phiên trước).
- **long (treo từ 20/09):** hai câu ở HOANG-T001. Có lẽ không còn cần vì đã bỏ hướng bắt lần mua.

## File đã sửa

Repo `cctv-goods-detection`, nhánh `docs/vault-and-recap-skill`:

- `document_vault/Recap/hoang/2026-09-24/RECAP_2026-09-24_1522.md` (mới, file này)
- Commit giúp phiên 10:48 (phiên này không sửa nội dung, trừ mục *commit* của recap 10:48):
  - `document_vault/Recap/hoang/2026-09-24/RECAP_2026-09-24_1048.md`
  - `docs/architecture/cost_model.py`
  - `docs/architecture/build_milk_aws.mjs`
  - `docs/architecture/milk-aws-architecture-demo.drawio`
  - `docs/architecture/milk-aws-architecture-demo.png`

Không commit: `document_vault/RESOURCES.md` (sửa dở từ 20/09).

Confluence:
- Tạo: HOANG-T005 `2523187`, Proposal `2523213`.
- Sửa: HOANG-T004 `2523154`, HOANG-P03 `2392147`, Cách dùng — Ticket `65928`.
- Phiên bản từng trang ở bảng trong mục *Đã xong*.

Google Sheet: không sửa (Nextstep 2 của T005 để hoang sửa tay). Google Drive: không sửa. Bộ nhớ agent: không sửa.

## commit

- `b39fc14` — Recap 24/09 10:48 va 15:22 - kien truc HOANG-P03, so do tong quan, cost_model; dong HOANG-T004; trang Proposal (HOANG-T005). Push lên `origin/docs/vault-and-recap-skill` (`273bd06..b39fc14`).
