# RECAP 2026-09-24 10:48

- owner: hoang
- created: 2026-09-24 10:48 +07:00
- plan/ticket đang chạy: HOANG-P03 (`in-progress`, mở phiên này) · HOANG-T004 (`in-progress`, chờ báo Sheet) · HOANG-P01 (`draft`) · HOANG-T001 (`draft`). Đóng trong phiên: HOANG-T002 `done`, HOANG-T003 `done`, HOANG-P02 `superseded`.

Tiếp nối [RECAP_2026-09-24_1015.md](RECAP_2026-09-24_1015.md). File mở lúc 10:48 với ba dòng đầu phiên; phần còn lại viết lúc chốt phiên, 12:20. Phiên này làm ba việc theo thứ tự. Đầu tiên dựng trang chung *Problems and risks*. Sau đó chốt kiến trúc AWS mới cho bài toán đếm tồn kho, từng bước bằng bảng so sánh, rồi ghi vào HOANG-P03 và trang Architecture design. Cuối cùng vẽ lại sơ đồ draw.io tổng quan.

## Đã đổi gì từ phiên trước

- **Người khác: không đổi gì.** `git pull` báo `Already up to date`. CQL `lastmodified >= now("-48h")` lúc 10:48 ra 11 trang, cả 11 do hoang sửa. Kiểm lại lúc 12:20 bằng `lastmodified >= now("-3h")`: 15 trang, cả 15 là trang hoang sửa trong hai phiên sáng nay. Claude rule vẫn v2.1.
- **Sheet (ProjectA) có dòng MeetingNote mới 24/09** (Hoàng, Long): *"Chỉ đếm tồn kho, bỏ hướng bắt lần mua. HOANG-P02 chuyển blocked. Đã sửa Project plan, Project info (HOANG-T002)"*. Đây là thông báo Sheet mà T002 còn chờ. Dòng *Prototype* có chủ hoang, ưu tiên P0, 24/09 → 30/09. Dòng *architecture design* vẫn trống chủ và trạng thái; *label data* vẫn `Bị chặn`, chủ `none`.
- **hoang báo đầu phiên:** T002 xong. File Data Dictionary **không cần** đưa lên Drive. Ba sơ đồ draw.io sẽ vẽ lại, bắt đầu từ sơ đồ thứ nhất.

## Đã xong

1. **Đóng HOANG-T002** `2359297` → `done` (v3, 10:49). Research `2293762` v2 (10:54) ghi rằng file Data Dictionary không đưa lên Drive, theo quyết định của hoang.
2. **Trang chung *Problems and risks*** `2326555`. Tạo lúc đầu là trang research của hoang, liệt kê vấn đề và rủi ro của sản phẩm theo yêu cầu Datagent 24/09. Sau đó chuyển thành trang chung qua **HOANG-T003** `2392117` (tạo 11:00, `done` 11:21): trang dời xuống dưới *Overview* `65797`, đổi tên thành *Problems and risks* (v2). Dòng của trang này ở *Cách dùng — Research* được gạch và ghi chỗ chuyển tới.
3. **Chốt kiến trúc mới**, hỏi hoang từng bước, mỗi bước kèm bảng số và nguồn. Chi tiết ở mục *Quyết định đã chốt*.
4. **Confluence.** Trang có sẵn sửa theo `local-id`; riêng Architecture design phải ghi đè cả trang (xem dòng của trang đó).

| Trang | id | Việc | Phiên bản |
|---|---|---|---|
| HOANG-T002 | `2359297` | `done`, Nhật ký | v3 |
| Research *Yêu cầu pilot của Datagent* | `2293762` | Ghi: Data Dictionary không đưa lên Drive | v2 |
| Problems and risks | `2326555` | Tạo, dời xuống dưới Overview `65797`, đổi tên | v2 |
| HOANG-T003 — Chuyển trang vấn đề và rủi ro thành trang chung | `2392117` | Tạo 11:00, `done` 11:21 | v3 |
| HOANG-P03 — Kiến trúc AWS cho hệ thống đếm tồn kho | `2392147` | Tạo 11:52 `in-progress`. Lần sửa 12:13: gạch phần sơ đồ tổng quan ở Nextstep 2, sửa "Kich bản A" thành "Kịch bản A" và "11:5x" thành "trước 11:52", thêm Nhật ký | v2 |
| HOANG-P02 | `1966083` | `blocked` → `superseded` (thay bằng P03), 11:55 | v5 |
| HOANG-T004 — Viết lại trang Architecture design theo HOANG-P03 | `2523154` | Tạo 11:56; bước 1 xong 12:04; chờ báo Sheet | v2 |
| Architecture design | `1802251` | **Viết lại toàn bộ** thành 11 mục, gửi bằng HTML lúc 12:04. Bản markdown bị công cụ từ chối ("would cause data loss") vì bản cũ có `rowspan`, macro note và resource. Nhật ký T004 ghi rõ đây là bỏ nội dung cũ có chủ ý, bản cũ còn ở v4 | v5 |
| Cách dùng — Ticket | `65928` | T002 `done`; thêm T003 rồi `done`; thêm T004 | v12 |
| Cách dùng — Research | `98546` | Thêm dòng trang rủi ro, sau đó gạch vì đã chuyển thành trang chung | v9 |
| Cách dùng — RoadMap | `98529` | P02 `superseded`; thêm P03 | v10 |

5. **Viết lại `docs/architecture/cost_model.py`** cho kiến trúc mới (bản cũ ở commit `518eeae`). Chạy `PYTHONIOENCODING=utf-8 python cost_model.py`, trích output nguyên văn:

```
  30 tiệm × 1 camera   A · cập nhật mỗi đêm             444   ( 12,600 ảnh xử lý ·    110 giờ GPU)
  30 tiệm × 1 camera   B · cập nhật trong ngày        1,019   ( 40,320 ảnh xử lý ·    420 giờ GPU)
  30 tiệm × 2 camera   A · cập nhật mỗi đêm             651   ( 25,200 ảnh xử lý ·    215 giờ GPU)
  30 tiệm × 2 camera   B · cập nhật trong ngày        1,534   ( 80,640 ảnh xử lý ·    672 giờ GPU)
```

Chi tiết 30 × 1, kịch bản A: xử lý $180, bảo mật $124, train $79, website $22, sao lưu Osaka $21, lưu trữ $16, thu nhận $2, kết quả $0,5. Giả định (trong `ASSUMPTIONS`): tiệm mở 14 giờ/ngày, chụp 15 phút/lần, bỏ 20% ảnh có người, ảnh 10 MB, 14 ảnh xử lý/camera/ngày, 30 giây/ảnh *(chưa đo)*, khởi động nguội 10 phút, ảnh đệm giữ 7 ngày, ảnh đã xử lý giữ 3 tháng, train 20 giờ/tháng, CVAT trên m7i.xlarge 176 giờ/tháng.

6. **Sơ đồ tổng quan vẽ lại xong, 12:13.** Script `docs/architecture/build_milk_aws.mjs` viết lại, ra `milk-aws-architecture-demo.drawio` và `.png` (5158×2463). `node build_milk_aws.mjs bake` in `VALIDATE: ok true, errors [], warnings [], advice []`. Đã soát bằng mắt ảnh render và các ảnh cắt cận cảnh: luồng chính chạy ngang từ tiệm qua bốn tầng tới website, không còn đường xuyên qua icon.
   - Nội dung: tiệm (OAK 4 S, phần mềm trên camera, WiFi hoặc 4G, camera thay thế); đội thực địa; bốn tầng (thu nhận · xử lý mỗi đêm · kết quả và lưu trữ · website); khối train lại 2 tuần; ô *chưa kiểm chứng*; dải bảo mật; dải vận hành; region Osaka sao lưu.
   - Cách chạy lại (từ `docs/architecture/`): `node build_milk_aws.mjs bake` rồi `DRAWIO_CLI="C:/Users/huyho/AppData/Local/Programs/draw.io/draw.io.exe" drawio-ai render milk-aws-architecture-demo.drawio -o milk-aws-architecture-demo.png --scale 1.5`.
   - Mẹo: nhãn nằm dưới icon, nên đường dọc nối hai icon xếp chồng luôn cắt qua nhãn. Dùng `route: { es, en }` để ghim đường vào cạnh bên. Đường dài sang Osaka làm rối hình, nên đã bỏ; ghi nguồn chép vào nhãn icon Osaka.
7. **Bộ nhớ agent:** sửa `aws-architecture-decisions-2026-09-23.md` (P02 `superseded` → P03) và thêm các quyết định 24/09.

## Đang dở

- **HOANG-T004 còn `in-progress`**, chờ đúng một việc: hoang báo trong Sheet rằng trang chung Architecture design đã viết lại. Nội dung gợi ý (đã đưa hoang trong chat):
  > Architecture design viết lại theo yêu cầu Datagent 24/09: đếm tồn kho từ ảnh kệ, xử lý mỗi đêm bằng SageMaker Async, sao lưu Osaka, ~$444/tháng cho 30 tiệm. Lý do và nguồn: HOANG-P03. Ticket HOANG-T004. Sơ đồ tổng quan mới: docs/architecture/milk-aws-architecture-demo.drawio

  Báo xong thì đóng T004: `status` = `done`, sửa `updated`, gạch dòng 2–3 ở Nextstep, thêm Nhật ký, sửa dòng T004 ở *Cách dùng — Ticket*.
- **HOANG-P03 còn `in-progress`.** Nextstep 2 còn hai sơ đồ, Nextstep 3 là câu hỏi cho Datagent. Hai sơ đồ `milk-aws-compute-detail.drawio/.png` và `milk-aws-security.drawio/.png`, cùng script `build_milk_compute.mjs` và `build_milk_security.mjs`, **vẫn là bản của P02** (video, KVS, EKS) và đã lỗi thời. Chưa sửa gì.
- **File draw.io trên Google Drive mà Sheet đang trỏ tới** (fileId `1noju22t8RJMBHEp_DnE-L6fr1r7PVp7c`) vẫn là bản cũ. Chưa hỏi xong hoang tự thay hay để agent cập nhật lên Drive. Việc này đưa nội dung ra ngoài nên phải hỏi trước.
- **Còn chưa kiểm chứng:** chỉ có S3 gateway endpoint đã đủ cho SageMaker Async bật network isolation chưa; 30 giây/ảnh trên L4 chưa đo; nguồn giá Viettel là trang đại lý (vietteldata.vn), không phải trang chính thức.
- **Nháp trong scratchpad của phiên** (sẽ mất): `architecture_design_v5.md`/`.html` (thân trang Architecture design v5; trang Confluence là bản gốc), các file JSON giá, ảnh cắt. Không cần giữ.
- `document_vault/RESOURCES.md` vẫn sửa dở, chưa commit từ 20/09 (phiên này không đụng).

## Bước kế tiếp

**Việc đầu tiên: hỏi hoang đã báo Sheet cho T004 chưa. Báo rồi thì đóng T004 theo mục *Đang dở*.**

Sau đó **vẽ lại sơ đồ chi tiết tầng xử lý** (`build_milk_compute.mjs` → `milk-aws-compute-detail.drawio`), theo mục *Xử lý mỗi đêm* của HOANG-P03. Luồng cần vẽ:

1. EventBridge Scheduler chạy mỗi đêm, gọi Lambda chọn ảnh tốt nhất mỗi giờ.
2. Lambda gửi ảnh cho SageMaker Async Inference: ml.g6.xlarge, 0 → 1 máy, network isolation, nằm trong VPC riêng chỉ có private subnet.
3. SageMaker đọc ảnh từ S3 qua S3 gateway endpoint.
4. Xong thì SageMaker báo SNS (xong/lỗi), SNS gọi Lambda ghi kết quả.
5. Lambda ghi kết quả vào Firehose → S3 Tables, và vào DynamoDB.

Trước khi viết script, đưa hoang danh sách nội dung sơ đồ để duyệt; hoang muốn đi từng bước. Xong thì validate, render (lệnh ở mục *Đã xong* 6), soát bằng mắt, rồi gạch dòng tương ứng ở Nextstep 2 của P03. Sau đó mới tới sơ đồ bảo mật (`build_milk_security.mjs`).

## Quyết định đã chốt

- **Danh sách service** (hoang chốt khoảng 11:20; ghi ở P03 và Architecture design v5).
  - **Giữ:** khung các tầng cũ và region Tokyo.
  - **Bỏ:** KVS, Lambda lấy khung, Kinesis Data Streams, HyperPod, endpoint `kinesis-streams`, xem camera trực tiếp.
  - **Thêm:**
    - Nhận ảnh: S3 Event → SQS → Lambda ghi nhận ảnh vào DynamoDB.
    - API tải lên (API Gateway + presigned URL) cho thẻ SD, kính và điện thoại.
    - Lệnh IoT "chụp ngay".
    - Train lại 2 tuần một lần: SageMaker Pipelines + EventBridge Scheduler; chỉ nhận model mới nếu điểm trên tập kiểm định cố định không giảm.
    - Tại camera: làm mờ mặt.
    - Cảnh báo camera: không có ảnh mới, lệch góc.
  - **Đổi:**
    - Phần mềm camera bỏ ảnh có người che kệ (trước đây là giữ đoạn có người).
    - Bốn bảng `cv_output`, `human_audit`, `sku_master`, `store_list` trên S3 Tables; Athena tính 12 KPI.
    - Công cụ gán nhãn thành bắt buộc.
    - Website: tồn kho, duyệt, 12 KPI, tải lên, xuất CV Output.
- **Camera: OAK 4 S là phương án chính.** OAK-D CM4 PoE và camera 2 MP sẵn có ở tiệm là phương án thay thế cho một số trường hợp. Lý do là độ phân giải trên kệ ở khoảng cách 2 m: OAK 4 S 30,2 px/cm; OAK-D CM4 14,8 px/cm; camera 2 MP (ống 2,8 mm) 3,2 px/cm.
- **Datagent xem tồn kho hằng ngày** (hoang trả lời). Hệ quả: thẻ SD với lịch thực địa 2 tuần/lần không đáp ứng được, phải có đường mạng từ tiệm.
- **Router 4G cho tiệm không có WiFi.** Bộ nhớ 128 GB trên camera làm đệm, thẻ SD là dự phòng. Nguồn giá:
  - Gói Viettel SD100: 100.000 đ/30 ngày, 2 GB/ngày (vietteldata.vn — đại lý).
  - Router TP-Link TL-MR100: 830.000–990.000 đ (meta.vn, philong.com.vn).
- **Xử lý bằng SageMaker Async Inference.**
  - Máy ml.g6.xlarge giá $1,6341/giờ ở Tokyo (mã `APN1-AsyncInf:ml.g6.xlarge`, bảng giá AWS Price List lấy lúc 2026-09-23T22:47Z). Về 0 máy khi rảnh.
  - So sánh đã đưa hoang:

    | Phương án | Kịch bản A (105 giờ GPU/tháng) | Kịch bản B (420 giờ) |
    |---|---|---|
    | EKS | $588 | $984 |
    | AWS Batch | $398 | $766 |
    | SageMaker Async | $172 | $686 |

  - Hoà vốn: Batch rẻ hơn SageMaker khi trên khoảng 590 giờ GPU/tháng, EKS khi trên khoảng 1.210 giờ.
- **Hệ quả của SageMaker Async** (đã báo hoang, đã đưa vào P03):
  - Bỏ EKS/KEDA, nên CVAT chạy trên một máy EC2 bằng Docker Compose.
  - Container bật network isolation nên không gọi được Firehose hay DynamoDB. Vì vậy thêm SNS → **Lambda ghi kết quả**, ngược với dòng "Lambda ghi DynamoDB: bỏ" trong danh sách chốt lúc đầu.
  - Không còn cần interface VPC endpoint, chỉ cần S3 gateway endpoint *(chưa kiểm chứng đủ chưa)*.
- **Cập nhật tồn kho mỗi đêm (kịch bản A).** Mỗi tiệm 14 ảnh/ngày (ảnh tốt nhất mỗi giờ), xử lý một đợt sau giờ đóng cửa. Lý do: đủ cho yêu cầu xem hằng ngày, và rẻ hơn kịch bản B khoảng 2,3 lần ($444 so với $1.019).
- **CVAT** làm công cụ gán nhãn (bỏ Label Studio).
- **Sao lưu sang Osaka là cần thiết** (hoang chốt). Gồm S3 Replication, S3 Tables replication, AWS Backup cho DynamoDB và KMS multi-Region key. Chính sách region chỉ cho Tokyo và Osaka.
- **Mở plan mới HOANG-P03, không viết lại P02.** P02 chuyển `superseded` để giữ lịch sử kiến trúc cũ.
- **Trang rủi ro là trang chung *Problems and risks*,** nằm dưới Overview, không nằm trong folder research của hoang. Theo yêu cầu của hoang: "biến nó thành 1 page chung".
- **Architecture design viết lại toàn bộ.** Trang cũ không còn đúng với bài toán mới, và công cụ không cho sửa từng phần trên nền bảng `rowspan` cũ. Bản cũ còn ở v4.

## Câu hỏi treo

- **hoang:**
  - Báo Sheet để đóng T004.
  - Thay file draw.io trên Drive mà Sheet trỏ tới (tự làm hay để agent làm).
  - Có vẽ tiếp sơ đồ chi tiết xử lý và sơ đồ bảo mật không.
  - Chọn đơn vị đếm (lẻ, lốc/thùng hay cả hai).
  - Dòng *architecture design* trong Sheet vẫn trống chủ và trạng thái (treo từ 23/09).
  - Có commit `RESOURCES.md` không (treo từ 20/09).
- **Datagent (qua hoang; ghi ở Nextstep 3 của P03):**
  - Mới từ phiên này: tần suất chụp; giờ đóng cửa; giữ ảnh thô bao lâu; bao nhiêu tiệm không có WiFi; tiệm nào cần 2 camera; ai giữ tài khoản AWS và ngân sách; ai trả tiền camera và 4G.
  - Còn từ phiên trước: danh sách top 50 SKU; `bounding_box` cho từng đơn vị hay cả nhóm; `count_accuracy_pct` khi người đếm bằng 0; `processing_time_ms` tính theo detection hay theo ảnh; khi nào gửi danh sách 30 tiệm và SKU Master kèm ảnh mẫu.
- **Tư vấn pháp lý (chưa giao ai, treo từ 23/09):** Luật 91/2025 — ảnh có mặt người (dù đã làm mờ tại camera), hồ sơ đánh giá tác động khi xử lý ở Tokyo.
- **Luxonis:** nguồn hàng, giá và thời gian giao OAK 4 S về Việt Nam. Giờ OAK 4 S là camera chính nên câu này cần hỏi.
- **team (khang, nghi, ngọc):** phạm vi nhánh B (thị trường) và C (giá trị dữ liệu), treo từ phiên trước.
- **long (treo từ 20/09):** hai câu ở HOANG-T001. Có lẽ không còn cần vì đã bỏ hướng bắt lần mua.

## File đã sửa

Repo `cctv-goods-detection`, nhánh `docs/vault-and-recap-skill`:

- `document_vault/Recap/hoang/2026-09-24/RECAP_2026-09-24_1048.md` (mới, file này)
- `docs/architecture/cost_model.py` (viết lại)
- `docs/architecture/build_milk_aws.mjs` (viết lại)
- `docs/architecture/milk-aws-architecture-demo.drawio` (vẽ lại)
- `docs/architecture/milk-aws-architecture-demo.png` (render lại)

Không commit: `document_vault/RESOURCES.md` (sửa dở từ 20/09).

Confluence: tạo Problems and risks `2326555`, HOANG-T003 `2392117`, HOANG-P03 `2392147`, HOANG-T004 `2523154`; sửa HOANG-T002 `2359297`, research `2293762`, HOANG-P02 `1966083`, Architecture design `1802251`, Cách dùng — Ticket `65928`, Research `98546`, RoadMap `98529`. Phiên bản ở bảng trong mục *Đã xong*. Google Sheet: không sửa. Google Drive: không sửa.

Ngoài repo: bộ nhớ agent trên máy hoang (`aws-architecture-decisions-2026-09-23.md`, `MEMORY.md`); nháp trong scratchpad của phiên (tạm, sẽ mất).

## commit

- chưa push — điền hash sau khi commit và push.
