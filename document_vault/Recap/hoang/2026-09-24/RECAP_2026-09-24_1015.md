# RECAP 2026-09-24 10:15

- owner: hoang
- created: 2026-09-24 10:15 +07:00
- plan/ticket đang chạy: HOANG-T002 (`in-progress`, mở phiên này) · HOANG-P02 (`blocked` từ phiên này) · HOANG-P01 (`draft`) · HOANG-T001 (`draft`)

Tiếp nối [RECAP_2026-09-23_2323.md](../2026-09-23/RECAP_2026-09-23_2323.md). File mở lúc 10:15 với ba dòng đầu phiên; phần còn lại viết lúc chốt phiên, 10:40. Phiên này đọc Data Dictionary mà Datagent gửi sáng 24/09, rồi đưa yêu cầu mới vào mọi trang Confluence liên quan. **Phiên sau bàn lại kiến trúc** theo yêu cầu mới.

## Đã đổi gì từ phiên trước

- **Người khác: không đổi gì.** `git pull` báo `Already up to date`. CQL `lastmodified >= now("-48h")` trên space ra đúng 3 trang, cả ba do hoang sửa tối 23/09 (HOANG-P02, Architecture design, Cách dùng — RoadMap). Năm người còn lại vẫn chưa có plan, ticket hay research nào.
- **Sheet không đổi** so với recap 23:23. Dòng *architecture design* vẫn trống chủ và trạng thái; *label data* vẫn `Bị chặn` với ghi chú *"chưa rõ requirement về ouput cụ thể … cần bàn thêm với client"*.
- **Đầu vào mới từ khách:** sáng 24/09 nhận `Datagent_Computer Vision_Data_Dictionary.xlsx` (6 sheet, chỉ có định nghĩa trường và giá trị ví dụ). Theo file này, pilot là **đếm tồn kho trên kệ theo SKU** tại 30 tiệm tạp hoá TP.HCM (sữa + mì ăn liền), có KPI bằng số. hoang hỏi lại Datagent và được trả lời hai điều: (a) *visible units* và *units on shelf* đều là hàng nhìn thấy được; (b) đơn vị đếm có thể là một trong hai (lẻ hoặc lốc/thùng) hoặc cả hai, team tự quyết.

## Đã xong

1. **Đọc và mô tả Data Dictionary** `D:\Datagent_Computer Vision_Data_Dictionary.xlsx` (17.417 byte, SHA-256 `91881471b4f44ae8034f3ab97447008b34c02d074e2a011725adce1c770e58f3`). Sáu sheet: README, Store List, SKU Master, CV Output, Human Audit, Accuracy Report. File chỉ có định nghĩa trường và giá trị ví dụ, chưa có dữ liệu thật. Đọc bằng openpyxl 3.1.5, cài tạm vào scratchpad của phiên vì Python trên máy hoang không có openpyxl. Dò từ khoá trên mọi ô: *purchase, customer, transaction, event, people, person* đều 0 lần; *sell* 1 lần ở `README!A8`, trong câu loại trừ sell-in.
2. **Hỏi hoang ba quyết định:** chỉ đếm tồn kho; HOANG-P02 chuyển `blocked`; hoang tự duyệt ticket sửa trang chung. Chi tiết ở mục *Quyết định đã chốt*.
3. **Confluence.** Mọi chỗ sửa trên trang có sẵn đều làm theo `local-id` (tham số `edits`), không ghi đè cả trang.

| Trang | id | Việc | Phiên bản |
|---|---|---|---|
| Research *2026-09-24 — Yêu cầu pilot của Datagent: đếm tồn kho trên kệ theo SKU* | `2293762` | Tạo mới trong Hoàng_Research. Nguồn chung cho yêu cầu: Kết luận, thông số pilot, bảng CV Output, 12 KPI, Bằng chứng (trích nguyên văn theo ô), Cái chưa biết (14 điểm) | v1 |
| HOANG-T002 — Đưa yêu cầu pilot của Datagent (24/09) vào Project plan và Project info | `2359297` | Tạo mới trong Hoàng_Ticket; Nhật ký ghi hoang tự duyệt | v1 → v2, `in-progress` |
| Project plan | `131382` | Mục 1 viết lại. Mục 2 thêm 4 dòng (Đầu ra, Tiêu chí nghiệm thu, Dữ liệu từ Datagent, Kiến trúc AWS) và sửa dòng Quy trình phân tích. Mục 3 sửa nhánh C, thêm ghi chú phạm vi B/C. Mục 4 gạch câu 1–2, ghi chú câu 4 và 6. Mục 5 gạch dòng tiêu chí bằng số, thêm câu vào dòng chi phí train, thêm mì ăn liền, kính Meta Ray-Ban, đơn vị đếm | v6 → v7 |
| Project info | `327685` | Viết lại *Bài toán*, *Giai đoạn hiện tại* và dòng Cập nhật | v6 → v7 |
| HOANG-P01 | `66482` | Khung cảnh báo dưới Mục tiêu, Nhật ký, `updated`; giữ `draft` | v5 → v6 |
| HOANG-T001 | `98895` | Nhật ký, `updated`; giữ `draft` | v4 → v5 |
| HOANG-P02 | `1966083` | `approved` → `blocked`; khung cảnh báo dưới Mục tiêu; Nhật ký liệt kê 6 chỗ lệch | v3 → v4 |
| Architecture design | `1802251` | Khung cảnh báo đầu trang "Đang xét lại — 24/09/2026", dòng Cập nhật | v3 → v4 |
| Cách dùng — RoadMap / Ticket / Research | `98529` / `65928` / `98546` | Chỉ mục: P02 `blocked`, mốc mới của P01 và T001, thêm T002, thêm trang research | v9 / v8 / v7 |

4. **Kiểm lại sau khi sửa**, đọc bản HTML: Project info vẫn giữ `<ol start="3">`; Architecture design vẫn còn các ô `rowspan`; HOANG-P02 có `status` = `blocked`, `updated` = 2026-09-24 10:29 +07:00; Project plan v7 khớp bản dry run.
5. **Bộ nhớ agent trên máy hoang:** thêm `datagent-requirements-2026-09-24.md`, `hoang-self-approves-shared-page-tickets.md`, `confluence-edit-by-local-id.md`; sửa `aws-architecture-decisions-2026-09-23.md` (P02 `blocked`, tiền đề cũ đã bỏ); cập nhật `MEMORY.md`.

## Đang dở

- **HOANG-T002 còn `in-progress`**, chờ đúng một việc: hoang báo trong Sheet rằng Project plan và Project info đã đổi (Claude rule mục 8). Nội dung gợi ý cho bảng MeetingNote đã đưa hoang trong chat. Báo xong thì đóng T002: `status` = `done`, sửa `updated`, gạch dòng 2–3 ở Nextstep, thêm Nhật ký, sửa dòng T002 trong chỉ mục *Cách dùng — Ticket*.
- **File Data Dictionary chỉ nằm trên máy hoang** (ổ D:). Trang research ghi "chưa có link dùng chung". Cần đưa lên Drive, thêm link vào Sheet, rồi sửa mục Bằng chứng của trang research.
- **Sơ đồ draw.io trong `docs/architecture/` và `cost_model.py` đã lỗi thời toàn bộ**, không chỉ chỗ `kvs → eks` như recap 23:23 ghi. Cả ba sơ đồ và mô hình chi phí dựng cho video + bắt lần mua (192 giờ video/camera/tháng, 100 sự kiện/camera/ngày). Chưa sửa gì, chờ kiến trúc mới.
- **Lệch giờ nhỏ giữa trang và Confluence.** Giờ ghi trong trang lấy từ `date` chạy trước khi soạn xong, nên sớm hơn giờ Confluence lưu khoảng 1 phút: research ghi 10:22, lưu 10:23; T002 10:23/10:24; Project plan 10:27/10:28; Project info 10:27/10:28; P01 10:27/10:29; T001 10:27/10:29; P02 10:29/10:30; Architecture design 10:29/10:30; ba chỉ mục và T002 v2 10:30/10:31.
- **openpyxl chỉ cài trong scratchpad của phiên này** (`pip install --target <scratchpad>/pylib openpyxl`, chạy với `PYTHONPATH=<scratchpad>/pylib`), sẽ mất. Phiên sau cần đọc xlsx thì cài lại theo cách đó, không cài vào Python chung.
- `document_vault/RESOURCES.md` vẫn sửa dở, chưa commit từ 20/09 (phiên này không đụng).

## Bước kế tiếp

**Việc đầu tiên: bàn lại kiến trúc với hoang theo yêu cầu đếm tồn kho.**

1. Trước khi hỏi, đọc trang research `2293762` (mục Kết luận và Cái chưa biết) và dòng Nhật ký 24/09 10:29 của HOANG-P02 (6 chỗ lệch).
2. Lập bảng cho từng tầng 0–8 của HOANG-P02: giữ, đổi hay bỏ, kèm số liệu và nguồn — hoang cần bảng số + nguồn trước khi quyết. Những chỗ gần như chắc phải đổi: thu nhận (video KVS → ảnh time-lapse), bộ lọc người tại camera (giờ người là vật che kệ), quy mô 30 tiệm × 1 camera, đường truyền khi tiệm không có WiFi (thẻ SD), ngưỡng ≤ 30 giây/ảnh, vòng train lại 2 tuần một lần.
3. Hỏi hoang từng câu một, theo thứ tự: ai lắp camera và CCTV chụp mấy phút một ảnh; kính Meta Ray-Ban có nằm trong pilot không; xử lý tại camera hay trên cloud; giữ ảnh thô bao lâu.
4. Chốt xong thì mở plan mới thay HOANG-P02 hoặc viết lại P02 (hoang chọn), rồi sửa trang Architecture design.

Trước bước 1: hỏi hoang đã báo Sheet chưa; rồi thì đóng HOANG-T002 (xem mục *Đang dở*).

Sau kiến trúc: viết lại hoặc thay HOANG-P01 bằng plan cho pipeline đếm tồn kho. S1 (định vị) và S4 (đọc nhãn) có thể dùng lại *(chưa kiểm chứng)*.

## Quyết định đã chốt

- **Chỉ đếm tồn kho trên kệ theo SKU, bỏ hướng bắt lần mua** — hoang chốt 24/09. Lý do: Data Dictionary là yêu cầu bằng văn bản đầu tiên của Datagent, chỉ định nghĩa việc đếm tồn kho (`README!A6`, `A12`, `A14`) và không nhắc tới lần mua.
- **HOANG-P02 `approved` → `blocked`** — hoang chọn. Lý do: tiền đề (video + bắt lần mua) đã bỏ; `blocked` báo cho người khác biết chưa nên xây tiếp (Prototype website đang phụ thuộc P02). Chưa chuyển `superseded` vì chưa có plan thay.
- **HOANG-P01 và HOANG-T001 giữ `draft`**, chỉ thêm Nhật ký (P01 thêm khung cảnh báo). Lý do: `superseded` phải ghi plan thay thế, mà chưa có.
- **hoang tự duyệt ticket sửa trang chung.** hoang nói: *"bây giờ tôi sẽ là người sửa chính cho cả team trước, vì trước giờ đều tôi làm nên tự sửa"*. Việc này khác quy định "phải có người khác duyệt" ở Cách dùng — Ticket; đã ghi vào Nhật ký T002. Quy định trên Confluence chưa sửa.
- **Datagent trả lời 24/09 (qua hoang):** *visible units* và *units on shelf* đều là hàng nhìn thấy được, nên AI và người đếm cùng một thứ; đơn vị đếm (lẻ, lốc/thùng hay cả hai) team tự chọn.
- **Trang research `2293762` là nguồn chung cho yêu cầu.** Project plan, Project info và các plan đều dẫn về đó. Câu đã trả lời ở Project plan mục 4 được gạch đi kèm chỗ chuyển tới, không xoá, để giữ số thứ tự câu.
- **Sửa trang Confluence có sẵn theo `local-id`, không ghi đè bằng markdown.** Lý do: bản markdown làm mất `start="3"` ở Project info và `rowspan` ở Architecture design.

## Câu hỏi treo

- **hoang:** báo Sheet để đóng HOANG-T002; đưa file Data Dictionary lên Drive và thêm link vào Sheet; chọn đơn vị đếm (lẻ, lốc/thùng hay cả hai); dòng *architecture design* trong Sheet vẫn trống chủ và trạng thái (treo từ 23/09); có commit `RESOURCES.md` không (treo từ 20/09).
- **hoang, cho buổi bàn kiến trúc:** ba câu còn mở của P02 (chốt A/B/C, CVAT hay Label Studio, có sao lưu Osaka không) cần xét lại. A/B/C gắn với KVS nên có thể không còn cần; công cụ gán nhãn vẫn cần cho vòng sửa lỗi của Datagent.
- **team (khang, nghi, ngọc):** phạm vi nhánh B (thị trường) và C (giá trị dữ liệu), vì `README!A8` ghi dự báo thị trường là phần của Datagent.
- **Datagent (qua hoang):** danh sách top 50 SKU theo sản lượng; tần suất chụp time-lapse; `bounding_box` một khung cho từng đơn vị hay cho cả nhóm; `count_accuracy_pct` tính thế nào khi người đếm bằng 0; `processing_time_ms` tính theo detection hay theo ảnh; một hay hai camera mỗi tiệm; ai lắp camera và lịch lắp; ai giữ tài khoản AWS và ngân sách; giữ ảnh thô bao lâu; có làm mờ mặt không; khi nào gửi danh sách 30 tiệm và SKU Master kèm ảnh mẫu.
- **Tư vấn pháp lý (chưa giao ai, treo từ 23/09):** Luật 91/2025 — ảnh có mặt người, hồ sơ đánh giá tác động nếu xử lý ở nước ngoài. Kính Meta Ray-Ban quay trong tiệm làm câu này gấp hơn.
- **long (treo từ 20/09):** hai câu ở HOANG-T001 (video camera cố định khác; tham số chạy ba clip MERL). Có thể không còn cần vì đã bỏ hướng bắt lần mua; xét lại khi viết plan mới.
- **Luxonis:** nguồn hàng OAK 4 S — chỉ cần hỏi nếu kiến trúc mới còn dùng OAK.

## File đã sửa

Repo `cctv-goods-detection`, nhánh `docs/vault-and-recap-skill`:

- `document_vault/Recap/hoang/2026-09-24/RECAP_2026-09-24_1015.md` (mới, file này)

Confluence: tạo research `2293762` và HOANG-T002 `2359297`; sửa Project plan `131382`, Project info `327685`, HOANG-P01 `66482`, HOANG-T001 `98895`, HOANG-P02 `1966083`, Architecture design `1802251`, Cách dùng — RoadMap `98529`, Cách dùng — Ticket `65928`, Cách dùng — Research `98546`. Phiên bản ở bảng trong mục *Đã xong*. Google Sheet: không sửa.

Ngoài repo: bộ nhớ agent trên máy hoang (4 file ở mục *Đã xong* cộng `MEMORY.md`); openpyxl trong scratchpad của phiên (tạm, sẽ mất).

## commit

chưa push — điền hash ngay sau khi push.
