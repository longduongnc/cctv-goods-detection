# RECAP 2026-09-23 23:23

- owner: hoang
- created: 2026-09-23 23:23 +07:00
- plan/ticket đang chạy: HOANG-P02 (`approved`, mới tạo phiên này) · HOANG-P01 (`draft`) và HOANG-T001 (`draft`) — không đụng tới

Tiếp nối [RECAP_2026-09-23_2021.md](RECAP_2026-09-23_2021.md). Phiên này biến thiết kế kiến trúc AWS thành ba sơ đồ draw.io, một mô hình chi phí, và hai trang Confluence (HOANG-P02 + trang tổng quan *Architecture design*).

## Đã đổi gì từ phiên trước

- **Người khác: không đổi gì.** `git pull` báo `Already up to date`. Trên Confluence, trang sửa gần nhất trước phiên này vẫn là Project plan v6 (22/09 09:43); năm người còn lại vẫn chưa có plan, ticket hay research nào.
- **Sheet không đổi** so với recap 20:21. Dòng *architecture design* vẫn trống chủ sở hữu và trạng thái; MeetingNote 23/09 vẫn như cũ.
- **Sai quy trình, ghi lại cho đúng sự thật:** ba dòng này viết lúc chốt phiên, không phải đầu phiên như Claude rule bước 5. Bước 4 (mở Sheet) cũng làm muộn — lúc ~22:30, trước khi ghi Confluence, nhưng sau khi đã tạo file trong `docs/architecture/`.

## Đã xong

### 1. Kiểm chứng tài liệu AWS mà thiết kế dựa vào

| Điểm | Kết quả | Nguồn |
|---|---|---|
| S3 Tables ở Tokyo | Có | https://docs.aws.amazon.com/general/latest/gr/s3.html |
| Firehose ghi vào S3 Tables | Có | https://docs.aws.amazon.com/firehose/latest/dev/apache-iceberg-destination.html |
| EKS Auto Mode tự cài driver NVIDIA | Có | https://docs.aws.amazon.com/eks/latest/userguide/auto-accelerated.html |
| KVS GetImages | Có; tối thiểu 200 ms/khung, tối đa 300 s mỗi lần gọi, ảnh ≤ 3840×2160 | https://docs.aws.amazon.com/kinesisvideostreams/latest/dg/API_reader_GetImages.html |
| Khoá camera vào đúng stream | Có, `${credentials-iot:ThingName}` | https://docs.aws.amazon.com/kinesisvideostreams/latest/dg/how-iot.html |
| **Ground Truth** | **Không dùng được** — nguyên văn *"Amazon SageMaker Ground Truth is no longer open to new customers"* | https://docs.aws.amazon.com/sagemaker/latest/dg/sms.html |
| HyperPod ở Tokyo | Có (`ap-northeast-1` trong danh sách) | https://docs.aws.amazon.com/sagemaker/latest/dg/sagemaker-hyperpod.html |
| **KVS VPC endpoint** | **Không dùng được cho pilot** — phải xin allowlist, *"10 streams allowed per customer"*, API băng thông 2 Mbps | https://docs.aws.amazon.com/kinesisvideostreams/latest/dg/vpce-landing.html |
| Endpoint cho EKS không internet | ec2, ecr.api, ecr.dkr, s3, logs, sts, oidc-eks, eks-auth, eks… | https://docs.aws.amazon.com/eks/latest/userguide/private-clusters.html |
| GuardDuty giám sát EKS Auto Mode | Có | https://docs.aws.amazon.com/guardduty/latest/ug/how-runtime-monitoring-works-eks.html |
| Control Tower khoá region | Có, control Region deny | https://docs.aws.amazon.com/controltower/latest/userguide/region-deny.html |

Hai kết quả âm làm đổi thiết kế: tầng Train dùng công cụ gán nhãn tự host; cụm EKS private không gọi thẳng KVS được → mở ba phương án (A Lambda ngoài VPC lấy khung → S3; B NAT Gateway; C xin allowlist — không đủ 20–80 camera).

### 2. Ba sơ đồ draw.io trong `docs/architecture/`

Dựng bằng `drawio-ai-kit` (skill `drawio-aws`), contract `bake`. Cả ba đều `drawio-ai validate` → `ok=True, errors=0, warnings=0, advice=0`, và đã render PNG xem bằng mắt.

- `milk-aws-architecture-demo.drawio` — bản tổng: cửa hàng → 5 cột (Thu nhận, Xử lý, Đường ống sự kiện, Lưu trữ, Website) → Train, dải Bảo mật, dải Bền, Osaka nét đứt.
- `milk-aws-compute-detail.drawio` — tầng Xử lý: VPC 3 AZ private không NAT, node GPU g6.xlarge (pod s1-locate, s4-read-label) + node CPU (orchestrator), KEDA, VPC endpoints, Lambda frame-fetcher ngoài VPC.
- `milk-aws-security.drawio` — landing zone: Management, Workloads OU (Prod 4 lớp, Dev), Security OU (Log Archive, Audit).

hoang nói link draw.io đã được gửi trên Google Sheet.

Mẹo đã gặp: `\n` trong nhãn bị XML biến thành dấu cách → dùng `<br>`; draw.io desktop có ở `C:\Users\huyho\AppData\Local\Programs\draw.io\draw.io.exe` nhưng không trong PATH → `DRAWIO_CLI=... drawio-ai render`; render `--scale 2` ra ảnh 6784 px bị mất dải giữa, dùng `--scale 1.5`.

### 3. Mô hình chi phí `docs/architecture/cost_model.py`

Giá On-Demand Tokyo tải từ AWS Price List API (`https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/<Service>/current/ap-northeast-1/index.json`, bảng giá công bố 11–22/09/2026), mỗi giá ghi usagetype. Output nguyên văn (`PYTHONIOENCODING=utf-8 python cost_model.py`):

```
Mỗi camera / tháng: gửi 192 giờ video = 173 GB · 691,200 khung · 3,000 sự kiện

TỔNG / THÁNG (USD, phương án mạng A: VPC endpoints)
                            P1 · S1 mọi khung (như hiện nay)         P2 · S1 chỉ khung then chốt         P3 · detector nhanh thay S1
S · 5 tiệm × 2 cam                       147,425  (115,276 GPU-h)                   4,386  (  2,576 GPU-h)                   1,387  (    213 GPU-h)
M · 10 tiệm × 2 cam                      293,875  (230,552 GPU-h)                   7,798  (  5,152 GPU-h)                   1,800  (    427 GPU-h)
L · 20 tiệm × 4 cam                    1,172,797  (922,210 GPU-h)                  28,488  ( 20,610 GPU-h)                   4,496  (  1,707 GPU-h)
...
MẠNG: A (VPC endpoints, không NAT) so với B (NAT Gateway) — quy mô M, P3
  A: VPC endpoints ×10 × 3 AZ             307.10   tổng  1,799.64
  B: NAT Gateway ×3                       166.78   tổng  1,659.32
```

Giá then chốt: g6.xlarge $1,1672/h (`APN1-BoxUsage:g6.xlarge`), phí Auto Mode $0,09104/h, VPC endpoint $0,014/h mỗi AZ, KVS GetImages $10/triệu ảnh. Giả định nằm trong `ASSUMPTIONS` — tất cả *(chưa kiểm chứng)*, quan trọng nhất là tốc độ detector P3 0,05 s/khung (chưa đo) và 100 sự kiện/camera/ngày.

### 4. Confluence

- Tạo [HOANG-P02 — Kiến trúc AWS cho hệ thống camera sữa](https://traditional-milk.atlassian.net/wiki/spaces/~7120204f8c1fdfbeb646c882494b9ab507ea3f/pages/1966083) (id `1966083`, v3) trong folder `131380`: thẻ đầu trang, Mục tiêu, Tình hình, Reason (9 tầng 0–8, 7 quyết định kèm nguồn, bảng kiểm chứng, chi phí, rủi ro), Nextstep 9 việc, Nhật ký 5 dòng. `status` = `approved` theo xác nhận của hoang.
- Thêm dòng HOANG-P02 vào chỉ mục [Cách dùng — RoadMap](https://traditional-milk.atlassian.net/wiki/spaces/~7120204f8c1fdfbeb646c882494b9ab507ea3f/pages/98529) (v8).
- Điền trang [Architecture design](https://traditional-milk.atlassian.net/wiki/spaces/~7120204f8c1fdfbeb646c882494b9ab507ea3f/pages/1802251) (id `1802251`, v3, hoang tạo trống) — bản tổng quan 11 mục cho team và Datagent, không có thẻ plan, chi tiết dẫn về HOANG-P02.

## Đang dở

- Không có job nào đang chạy; không mở máy GPU.
- **Bản tổng draw.io đã lỗi thời ở một chỗ:** vẫn vẽ `kvs → eks "GetImages"` (EKS gọi thẳng KVS) — mâu thuẫn với bản compute và HOANG-P02. Sửa trong `build_milk_aws.mjs` sau khi chốt A/B/C. Dải "Bảo mật" trong bản tổng cũng mới 9 dịch vụ, chưa khớp bản chi tiết.
- **Ba script `build_milk_*.mjs` import kit theo đường dẫn tuyệt đối trên máy hoang** (`file:///C:/Users/huyho/AppData/Roaming/npm/node_modules/drawio-ai-kit/...`) — máy khác chạy không được nếu không sửa. Chạy lại: `node build_milk_aws.mjs bake` rồi `DRAWIO_CLI=".../draw.io.exe" drawio-ai render <file>.drawio -o <file>.png --scale 1.5`.
- Trong HOANG-P02, trường `created` ghi 22:36 nhưng Confluence ghi thời điểm tạo là 22:39 (15:39 UTC) — lệch 3 phút vì `date` chạy trước khi soạn xong trang.
- Dòng HOANG-P02 trong chỉ mục ghi `Cập nhật` 22:36, trang đã `updated` 22:55 (chỉ thêm Nhật ký, không đổi trạng thái).
- `document_vault/RESOURCES.md` vẫn sửa dở chưa commit từ 20/09 (không đụng phiên này).

## Bước kế tiếp

**Việc đầu tiên:** hỏi hoang chốt đường lấy khung từ KVS (A Lambda ngoài VPC → S3 · B NAT Gateway · C allowlist), trình lại bảng: A giữ EKS kín nhưng tốn VPC endpoint $307/tháng; B $167/tháng nhưng mở đường ra internet. Chốt xong thì sửa `docs/architecture/build_milk_aws.mjs` (bỏ link `kvs → eks`, thêm Lambda frame-fetcher + S3 frames, cập nhật dải Bảo mật), dựng lại, và ghi dòng Nhật ký vào HOANG-P02.

Sau đó:

1. Đo S1 (LocateAnything-3B) và S4 (Qwen2.5-VL-7B) trên g6.xlarge (L4), sửa `s1_s_per_frame` / `s4_s_per_crop` trong `cost_model.py`, tính lại P2.
2. Thử nghiệm ngưỡng px/cm bằng ảnh điện thoại (treo từ recap 20:21).

## Quyết định đã chốt

- **HOANG-P02 `approved`.** hoang là trưởng nhóm, xác nhận thiết kế đã được thảo luận và duyệt trong nhóm. Reviewer ghi "team"; chưa có biên bản họp.
- **Trang Confluence chỉ có chữ và bảng;** sơ đồ draw.io nằm ở link trong Sheet (hoang).
- **Chín tầng, đánh số 0–8:** tách "Đường ống sự kiện" thành tầng 3 riêng cho khớp sơ đồ tổng.
- **Tách pod model khỏi pod điều phối:** S1 chậm hơn S4 ~13 lần (42 s so với 3,2 s, HOANG-P01) nên cần số máy GPU khác nhau.
- **Không dùng Ground Truth:** đã đóng với khách mới. Dùng công cụ tự host (CVAT hoặc Label Studio — chưa chọn).
- **P1 (S1 mọi khung) bị loại:** ~316 GPU chạy liên tục cho 20 camera.
- **Sơ đồ dựng bằng contract `bake`:** hết góp ý nhãn trên đường gấp khúc; đổi lại kéo icon trong draw.io phải Clear Waypoints.

## Câu hỏi treo

- **hoang:** chốt A/B/C; chọn CVAT hay Label Studio; có sao lưu Osaka không; có commit `docs/architecture/` lên repo không; có commit `RESOURCES.md` không (treo từ 20/09); cập nhật dòng *architecture design* trong Sheet (chủ, trạng thái).
- **team (hoang, long):** P2 hay đầu tư detector nhanh (P3) — sau khi đo trên L4.
- **Datagent (qua hoang):** ai sở hữu tài khoản AWS và ngân sách; giữ video thô bao lâu; chấp nhận độ trễ bao lâu.
- **Tư vấn pháp lý (chưa giao ai):** Luật 91/2025 — ảnh có mặt người có phải dữ liệu sinh trắc; hồ sơ đánh giá tác động khi xử lý ở Tokyo; có cần làm mờ mặt.
- **long (treo từ 20/09):** video camera cố định nào khác; tham số chạy ba clip MERL.
- **Project plan mục 5** vẫn ghi "Chi phí và hạ tầng train — chưa bàn" — sửa trang chung cần ticket + báo Sheet (Claude rule mục 8); chưa mở ticket.
- **Luxonis:** nguồn hàng OAK 4 S (trang bán ghi còn 3 chiếc).

## File đã sửa

Repo `cctv-goods-detection`, nhánh `docs/vault-and-recap-skill`:

- `docs/architecture/build_milk_aws.mjs`, `build_milk_compute.mjs`, `build_milk_security.mjs` (mới)
- `docs/architecture/milk-aws-architecture-demo.drawio` / `.png` (mới)
- `docs/architecture/milk-aws-compute-detail.drawio` / `.png` (mới)
- `docs/architecture/milk-aws-security.drawio` / `.png` (mới)
- `docs/architecture/cost_model.py` (mới; `__pycache__/` bị `.gitignore` bỏ qua)
- `document_vault/Recap/hoang/2026-09-23/RECAP_2026-09-23_2323.md` (file này)

Confluence: tạo HOANG-P02 (`1966083`, v1→v3); sửa *Cách dùng — RoadMap* (`98529`, v7→v8, chỉ thêm một dòng chỉ mục); điền *Architecture design* (`1802251`, v2→v3). Google Sheet: không sửa.

Ngoài repo: bộ nhớ agent trên máy hoang — cập nhật `aws-architecture-decisions-2026-09-23.md` (Ground Truth đóng, KVS VPC endpoint 10 stream, mô tả camera OAK). File giá tải về nằm trong scratchpad của phiên (sẽ mất), giá cần dùng đã chép vào `cost_model.py`.

## commit

- commit: `518eeaed6a0ef118786094c1c4cb80de7b0f7b38` — đã push lên `origin/docs/vault-and-recap-skill` (`214c32b..518eeae`). Gồm file recap này và toàn bộ `docs/architecture/` (hoang chọn). `RESOURCES.md` vẫn để nguyên, chưa commit.
